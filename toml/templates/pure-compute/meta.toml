# Template identity + signing metadata.
name = "pure-compute"
template_base = "base-confined"
maintainer_key = "kennel-maint-2026"
threat_catalogue_version = "0.4"
last_reviewed = "2026-07-10"
description = "The COMPUTE leg of the spawn-target split: a workload-optional cage that runs a caller-supplied command over coreutils + Python and reaches nothing (no net, no fs write)."
