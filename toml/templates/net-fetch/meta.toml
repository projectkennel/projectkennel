# Template identity + signing metadata.
name = "net-fetch"
template_base = "base-confined"
maintainer_key = "kennel-maint-2026"
threat_catalogue_version = "0.4"
last_reviewed = "2026-07-10"
description = "The NET leg of the spawn-target split: a workload-optional cage that fetches from a signed destination set via constrained proxy egress, no fs write."
