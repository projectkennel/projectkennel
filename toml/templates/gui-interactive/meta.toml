# Template identity + signing metadata.
name = "gui-interactive"
template_base = "interactive"
maintainer_key = "kennel-maint-2026"
threat_catalogue_version = "0.5"
last_reviewed = "2026-06-30"
description = "A single confined GUI app, relayed into the broker's nested compositor over the mesh."
