# Template identity + signing metadata.
name = "base-confined"
version = "1"
# Root template — no parent.
template_base = ""
# The signing key this version is signed under (id in the template trust store). policy.toml carries the matching [signature]; verify with
# `kennel validate --require-signed` against keys/kennel-maint-2026.pub.
maintainer_key = "kennel-maint-2026"
threat_catalogue_version = "0.4"
last_reviewed = "2026-06-02"
description = "The factored root of every confined template; not used directly."
