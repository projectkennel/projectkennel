# Template identity + signing metadata.
name = "echo-tool"
version = "1"
template_base = "base-confined@v1"
maintainer_key = "kennel-maint-2026"
threat_catalogue_version = "0.4"
last_reviewed = "2026-06-21"
description = "Minimal single-leg SPAWN target: echo stdin→stdout via /bin/cat (reaches nothing). The spawn-roundtrip e2e proves the channel reaches a running sibling."
